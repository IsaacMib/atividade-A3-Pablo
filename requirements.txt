# ============================================
# NEUROATHENA - Requirements Consolidados
# ============================================
# Django + Wagtail CMS para triagem de autismo
# Python 3.12+ | PostgreSQL | Wagtail 7.x

# --- CORE FRAMEWORK ---
Django>=5.1,<5.2
wagtail>=7.0,<8.0

# --- CONFIGURAÇÃO & AMBIENTE ---
python-decouple==3.8
django-dotenv==1.4.2
dj-database-url==2.1.0

# --- EXTENSÕES WAGTAIL ---
wagtail-font-awesome-svg>=1,<2
wagtail-color-panel==1.6.0

# --- FERRAMENTAS DJANGO ---
django-extensions==3.2.3
django-webpack-loader==3.1.1
django-browser-reload==1.17.0
django-debug-toolbar>=4.2,<5

# --- SEGURANÇA ---
django-csp==3.7
django-allauth==65.10.0
jwt==1.4.0

# --- UTILITÁRIOS ---
python-dateutil==2.9.0
python-magic==0.4.27

# --- BANCO DE DADOS (escolha uma opção) ---
# PostgreSQL (produção - Linux/Mac)
# psycopg[binary]>=3.1,<3.2

# PostgreSQL (Windows - mais estável)
psycopg2-binary>=2.9,<3.0

# --- CACHE & PERFORMANCE ---
django-redis>=5.4,<6.0
whitenoise>=6.6,<7.0

# --- TESTES ---
pytest>=7.4,<8.0
pytest-django>=4.5,<5.0
pytest-cov>=4.1,<5.0
coverage>=7.0,<8.0
factory-boy>=3.3,<4.0
faker>=20.0,<21.0

# --- QUALIDADE DE CÓDIGO (opcional) ---
# black>=22.3
# ruff>=0.0.290

# --- WINDOWS ESPECÍFICO ---
# python-magic-bin (substitui python-magic no Windows)
