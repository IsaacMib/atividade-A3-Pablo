# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY=django-insecure-!tof5-2xs!cufi3dm!byb^4y8_!#+6a%z=_n!d)_3_z@727uxq

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG=True

# Ambiente: dev, staging, production
AMBIENTE=dev

# Versão da aplicação
APP_VERSION=1.0.0

# Hosts permitidos (separados por vírgula)
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (para produção com PostgreSQL)
# DATABASE_URL=postgres://user:password@localhost:5432/gestao_estoque

# Email Configuration
EMAIL_HOST=localhost
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@gestaoestoque.com

# Redis (para cache e sessões em produção)
# REDIS_URL=redis://localhost:6379/0

# Security Settings (produção)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# Admin URL (customize em produção por segurança)
ADMIN_URL=admin/
