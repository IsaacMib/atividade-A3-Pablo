from django.urls import path

app_name = 'documentos'

urlpatterns = [
    # URLs específicas do app documentos (se necessário no futuro)
    # As URLs de visualização de documentos estão em core.urls
]
